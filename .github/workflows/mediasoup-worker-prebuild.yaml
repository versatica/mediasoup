name: mediasoup-worker-prebuild

on:
  push:
    tags:
      - "*.*.*"

jobs:
  ci:
    strategy:
      fail-fast: false
      matrix:
        build:
          - os: ubuntu-22.04
          - os: macos-12
          - os: windows-2022

        node:
          - 16

    runs-on: ${{ matrix.build.os }}

    env:
      CC: ${{ matrix.build.cc }}
      CXX: ${{ matrix.build.cxx }}

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Node.js
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node }}

      # We need to install some NPM production deps for npm-scripts.js to work.
      - run: npm run ci --ignore-scripts --production
      - run: npm run worker:build
      # Publish prebuild binaries on tag.
      - run: npm run worker:prebuild
      - name: Upload mediasoup-worker prebuilt
        uses: softprops/action-gh-release@v1
        with:
          files: worker/prebuild/mediasoup-worker-*.tgz
