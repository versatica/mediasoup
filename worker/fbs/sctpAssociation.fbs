namespace FBS.SctpAssociation;

enum SctpState: uint8 {
    NEW = 0,
    CONNECTING,
    CONNECTED,
    FAILED,
    CLOSED,
}

table SctpNegotiatedCapabilities {
    negotiated_max_outbound_streams: uint16;
    negotiated_max_inbound_streams: uint16;
}
