include "transport.fbs";

namespace FBS.DirectTransport;

table DirectTransportOptions {
  base:FBS.Transport.BaseTransportOptions (required);
}

table DumpResponse {
  base:FBS.Transport.BaseTransportDump (required);
}

// Notifications from Worker.

table RtcpNotification {
  data:[ubyte] (required);
}
