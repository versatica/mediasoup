include "transport.fbs";
include "webrtcserver.fbs";
include "worker.fbs";

namespace FBS.Response;

union Body {
  FBS.Worker.WorkerDump,
  FBS.Worker.ResourceUsage,
  FBS.WebRtcServer.WebRtcServerDump,
  FBS.Transport.TransportDump,
  FBS.Transport.ConsumeResponse,
}

table Response {
  id:uint32;
  accepted:bool;
  body:Body;
}
